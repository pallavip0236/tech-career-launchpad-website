```markdown
## CONTEXT

This project is a modern **front-end web application**, likely a marketing site, landing page, or informational portal, built for rapid development and a polished user experience. It features a rich set of pre-built, customizable UI components to ensure design consistency and accelerate development.

### Tech Stack & Architecture

*   **Frontend Framework**: React 18
*   **Language**: TypeScript
*   **Build Tool**: Vite (for fast development and bundling)
*   **Styling**: Tailwind CSS (utility-first approach) with PostCSS and `tailwindcss-animate`
*   **UI Library**: shadcn/ui (based on Radix UI primitives) for accessible, themeable, and customizable components.
*   **Routing**: React Router DOM for client-side navigation.
*   **Internationalization**: react-i18next (for multi-language support).
*   **Form Management**: React Hook Form with Zod for schema validation.
*   **Server State Management**: `@tanstack/react-query` (suggests handling of data fetching/caching).
*   **Client State Management**: Zustand (for lightweight global client-side state management).
*   **Architecture**: Single Page Application (SPA) with a strong component-based structure. Application views are defined in `src/pages`, composed of reusable components from `src/components`, which in turn utilize foundational UI elements from `src/components/ui`.

### Key Directories & Purpose

*   **`src/`**: Contains all core application source code.
    *   **`src/components/`**: Reusable React components that compose larger sections of pages (e.g., `Hero`, `Navigation`, `Footer`).
    *   **`src/components/ui/`**: Directory for shadcn/ui components. These are highly customizable, accessible UI primitives (e.g., `button`, `dialog`, `form`), typically copied into the project rather than installed as a direct dependency.
    *   **`src/pages/`**: Top-level components representing distinct application views or routes (e.g., `Index.tsx`, `NotFound.tsx`).
    *   **`src/hooks/`**: Custom React hooks for encapsulating and reusing stateful logic or side effects.
    *   **`src/lib/`**: Utility functions, helper modules, and other shared logic (`utils.ts`).
    *   **`src/store/`**: Centralized Zustand stores for global client-side state.
    *   **`src/i18n/`**: Internationalization configuration and language-related utilities.
    *   **`src/main.tsx`**: The main entry point for the React application.
*   **`public/`**: Static assets that are served directly by the web server (e.g., images, `robots.txt`, translation files in `locales/`).
*   **`tailwind.config.ts`**: Configuration for Tailwind CSS.
*   **`vite.config.ts`**: Configuration for the Vite build tool.

### Important Conventions & Patterns

*   **Component-Driven Development**: Emphasizes building UI in isolated, reusable components.
*   **Utility-First CSS**: Styling is primarily achieved through Tailwind CSS classes, promoting a consistent design system.
*   **Strong Typing**: The entire codebase leverages TypeScript for type safety, improved readability, and maintainability.
*   **shadcn/ui Customization**: UI components from shadcn/ui are integrated by being copied into the `src/components/ui` directory, allowing for deep customization and local control over their appearance and behavior without ejecting from a library.
*   **Zustand for Global State**: Leverages Zustand for managing application-wide client-side state, ensuring a clear separation from server state.
*   **Internationalization (i18n)**: Implemented using `react-i18next` to support multiple languages, with translation files typically stored in `public/locales` and managed via `src/i18n` configuration.
*   **Lovable Platform Integration**: The project is indicated as a "Lovable project" (from `README.md` and `lovable-tagger` dev dependency), implying it is managed or generated by the Lovable platform, which may influence specific development, deployment, and content editing workflows.
```