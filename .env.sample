# Application Configuration
VITE_APP_NAME="Your App Name" # The name of your application, displayed in the browser tab and UI.

# API Configuration
VITE_API_BASE_URL="http://localhost:3000/api/v1" # The base URL for your backend API.

# Authentication Configuration
VITE_AUTH_TOKEN_KEY="your_auth_token_key_name" # The key used to store the authentication token in cookies (e.g., "accessToken").